import React, { Component } from 'react'

const Context = React.create

export class Provider extends Component {
    render() {
        return (
            <Context.Provider>
            
            </Context.Provider>
        )
    }
}
